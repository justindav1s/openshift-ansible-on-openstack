---
- hosts: openstack-server
  vars:
  remote_user: justin
  become: yes
  become_method: sudo
  tasks:
  - name: create project
    os_project:
      cloud: default
      endpoint_type: admin
      state: present
      name: openshift

  - name: create user
    os_user:
      state: present
      cloud: default
      endpoint_type: admin
      domain: default
      name: openshift
      password: openshift
      default_project: openshift